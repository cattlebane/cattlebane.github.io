Player versions:
: Development / Encoder version . Player / Parsing version . internal iteration version

: 1.1.0 -> uses flipbook_convert_tofbv_2
		-> encodes all frames the same
		-> fbv format is {frames,fps,time,width,height};;;frame_base64:::

: 1.2.0 -> uses flipbook_convert_tofbv_4
		-> encodes mulitple types of frames
		-> encodes high quality end frame
		-> fbv format is {frames,fps,time,width,height};;;WIDTHxHEIGHT)frame_base64:::